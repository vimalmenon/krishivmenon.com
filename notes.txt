Finished Task:
  Add library for notes
  Quality: Minimize Eslint errors
  Auth: Ability to Login with Google Auth
  Auth: Ability to refresh token
  Responsive UI : Basic Responsive page 
  GitHub Action : Set up quality check on TSC and eslint
  GitHub Action : Automate Deploy and add Tag
  UI : Small Screen not to show hamburger menu if the user is not logged in
  Error if the .env file is not created
  Set up env file in the deployment

High Priority:
  Testing of application (React testing library)
  Create a page to update notes (WIP)
  Ability to Logout with AWS cognito
  Make the Website HTTPS
  Need to work on certificate
  Redirect apis from api.krishivmenon.com
  Need to work on logout of auth
  Improve UI
  Responsive UI on Notes
  Responsive UI on Gallery
  First time Refresh token is failing (Bug)
  Need to build unauthorized page
  Need to create "Not Authorized" page
  Need to show user detail in the Page
Medium Priority
  Pre signed image on request
  Need to create a Tags for notes and Library
  Create content for Home page
  Need to build 400 Page
  Need to build 500 page
Less Priority
  Setting up sonar
  Search for library for Image viewer
  Add notes for image
  Logging for S3 Bucket for static website


https://docs.aws.amazon.com/cognito/latest/developerguide/token-endpoint.html
https://docs.aws.amazon.com/cognito/latest/developerguide/logout-endpoint.html
https://mui.com/joy-ui/getting-started/templates/files/

KM - Krishiv Menon
VM - Vimal Menon
NK - Nitya Kurup

Table Name : Application
  appKey        : string
  sortKey       : string
  createdBy     : string
  createdDate   : string
  updatedDate   : string

IFile:
  id: string
  name: string;
  path: string
  metadata?: Record<string, string>;
  label: string;
  type: string;
  private: boolean;
  orphan: boolean;

IFolder:
  id: string
  content: string[]
  alias: string;
  type: string;
  private: boolean;

INote:
  title: string;
  content: string;
  metadata: Record<string,string>
